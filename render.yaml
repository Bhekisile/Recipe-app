databases:
  - name: recipeApp
    databaseName: recipe_app_557j
    user: recipe_app_557j_user

services:
  - type: web
    name: RecipeApp
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: recipeApp
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false
